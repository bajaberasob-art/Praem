[project]
name = "python-discord-bot"
version = "0.1.0"
description = "A starter Discord bot built with Python and discord.py"
requires-python = ">=3.11"
dependencies = [
    "discord.py==2.7.1",
    "aiosqlite>=0.19.0",
]
